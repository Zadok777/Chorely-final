// Bottom padding the tab screens add to their scroll content so the last row
// clears the floating glass TabBar (which is position: absolute and therefore
// doesn't push content up on its own). Tuned to the pill height + its bottom
// inset padding; a little generous so nothing hides behind the bar.
export const TAB_BAR_CLEARANCE = 120;
